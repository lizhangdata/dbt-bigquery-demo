select "{{ target.name }}" as target
,"{{ target.schema }}" as schema
,"{{ target.type }}" as type
,"{{ target.profile_name }}" as profile
,"{{ target.dataset }}" as dataset
,"{{ target.project }}" as project